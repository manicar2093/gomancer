{{ define "pkg_config_config.go" }}// Package config contains a struct with all your API configs
package config

import (
    "{{.ModuleName}}/core"
    "{{.ModuleName}}/core/connections"
)

type Config struct {
    core.Config
    connections.DatabaseConnectionConfig
}
{{ end }}
